#include <stdio.h>
/**
 * main - check the code
 *@argc:value to be ecaluated.
 *@argv: value to be evaluated
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
